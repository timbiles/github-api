import React from 'react';
import styled from 'styled-components';

const nav = () => {
    return (
        <Container>
                nav
        </Container>
    );
};

export default nav;

const Container = styled.div`
    display: flex;
    width: 100vw;
    background: #0F7173;
    color: #fff;
`