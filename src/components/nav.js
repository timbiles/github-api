import React from 'react';
import styled from 'styled-components';

const nav = () => {
    return (
        <Container>
                GitHub SearchBar
        </Container>
    );
};

export default nav;

const Container = styled.div`
    display: flex;
    width: 100vw;
    height: 10vh;
    background: #0F7173;
    color: #fff;
`